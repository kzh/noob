apiVersion: v1
kind: Service
metadata:
  name: {{ template "noob.fullname" . }}
  labels:
    app: {{ template "noob.name" . }}
    chart: {{ template "noob.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "noob.name" . }}
    release: {{ .Release.Name }}
